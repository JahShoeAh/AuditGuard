# ============================================================
# AuditGuard Agent Credentials — Hedera Testnet
# Copy this file to .env — these are shared testnet keys, safe to commit.
# NOTE: root .env is authoritative for *_ACCOUNT_ID / *_PRIVATE_KEY by default.
# This file should be used for agent runtime tuning; credential overrides are ignored unless
# ALLOW_AGENT_ENV_CREDENTIAL_OVERRIDE=true.
# ============================================================

# Shared fallback (used if per-agent keys are missing)
OPERATOR_ACCOUNT_ID=0.0.7951944
OPERATOR_PRIVATE_KEY=0x

# Agent 1: Scanner
SCANNER_ACCOUNT_ID=0.0.7951944
SCANNER_PRIVATE_KEY=0x

# Agent 2: Static Analysis
STATIC_ACCOUNT_ID=0.0.7951945
STATIC_PRIVATE_KEY=0x

# Agent 3: Fuzzer
FUZZER_ACCOUNT_ID=0.0.7951946
FUZZER_PRIVATE_KEY=0x

# Agent 4: LLM Contextual
LLM_ACCOUNT_ID=0.0.7951947
LLM_PRIVATE_KEY=0x

# Agent 5: Dependency
DEPENDENCY_ACCOUNT_ID=0.0.7951948
DEPENDENCY_PRIVATE_KEY=0x

# Agent 6: Report
REPORT_ACCOUNT_ID=0.0.7951949
REPORT_PRIVATE_KEY=0x

# Agent 7: Alert
ALERT_ACCOUNT_ID=0.0.7951955
ALERT_PRIVATE_KEY=0x

# Optional
DISCORD_WEBHOOK_URL=

# 0g Compute Network — LLM inference via @0glabs/0g-serving-broker SDK
# ZG_DEPOSIT_AMOUNT — Tokens to pre-deposit for inference credits
# ZG_ENABLED=false  — Force mock fallback (CI / offline dev)
ZG_PRIVATE_KEY=
ZG_RPC_URL=https://evmrpc-testnet.0g.ai
ZG_PROVIDER_ADDRESS=0xa48f01287233509FD694a22Bf840225062E67836
ZG_MODEL=qwen/qwen-2.5-7b-instruct
ZG_DEPOSIT_AMOUNT=1
ZG_ENABLED=true
ZG_REQUIRED_IN_LIVE=true
# pinned=use configured model (alias-safe), auto=use provider metadata model, hybrid=prefer configured then auto-correct
ZG_PROVIDER_MODE=pinned
ZG_HEALTHCHECK_TIMEOUT_MS=15000
ZG_REQUEST_TIMEOUT_MS=30000
ZG_MAX_INIT_RETRIES=2
ZG_PROBE_AT_STARTUP=true
ZG_MIN_LEDGER_CREDITS=1
