Before we start the deliverables, we need to finish certain tasks. Please complete the following:
Our end goal is to get the agents to successfully win a bid and get assigned the contract.
Right now, when an auction completes, we get a code on the lines of "Winner_selected_fallback". 
I'm not sure what that does, but I'm assuming a winner isn't chosen, and defaults to a single agent. 
Once an agent is correctly assinged a job, it needs to then actually process the contract and generate
an audit report. This needs to be in the format of a markdown file, and needs to get uploaded to the front end
associated with the address of the person who deployed it. This needs to be viewable only after purchasing. 

DO NOT MAKE ANY ASSUMPTIONS, ASK FOR MY INPUT FIRST.