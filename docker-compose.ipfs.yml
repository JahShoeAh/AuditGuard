version: "3.8"

services:
  ipfs:
    image: ipfs/kubo:v0.24.0
    ports:
      - "5001:5001"
      - "8080:8080"
    volumes:
      - ipfs_data:/data/ipfs
    environment:
      - IPFS_PROFILE=server

volumes:
  ipfs_data:
