{
  "buildCommand": "cd ../.. && npm ci --workspaces --include-workspace-root && npm --prefix packages/dashboard run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
